"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var superagent = require("superagent");
var scryfall_error_1 = __importDefault(require("../../models/scryfall-error"));
function get(url) {
    return superagent.get(url);
}
function post(url, body) {
    return superagent.post(url).send(body);
}
function sendRequest(options) {
    var requestPromise;
    if (options.method === "post" && options.body) {
        requestPromise = post(options.url, options.body);
    }
    else {
        requestPromise = get(options.url);
    }
    return requestPromise.set("Accept", "application/json").then(function (response) {
        var responseBody;
        try {
            responseBody = JSON.parse(response.text);
        }
        catch (parsingError) {
            return Promise.reject(new scryfall_error_1.default({
                message: "Could not parse response from Scryfall.",
                thrownError: parsingError,
            }));
        }
        if (responseBody.object === "error") {
            return Promise.reject(new scryfall_error_1.default(responseBody));
        }
        return responseBody;
    });
}
exports.default = sendRequest;
